---
'@ampolic/ui': patch
---

FaqList: the card now hugs its content — the height reservation moved to an
unstyled outer frame, so leftover ghost space blends into the page background
instead of showing as empty card.
